/**
 * The site isn't deployed yet (see CLAUDE.md). Sharing needs a URL to point at,
 * so it lives behind this one constant — swap it in once a real domain exists.
 */
export const SITE_URL = 'https://legendscollide.app';
