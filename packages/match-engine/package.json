{
  "name": "@fm/match-engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic football match simulation. Consumed by the web app through the MatchEngine interface; intended to be replaced by a Rust/WASM implementation.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
